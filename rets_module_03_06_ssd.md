# 시스템 화면 설계서 (SSD)

**문서명:** 시스템 화면 설계서 (System Screen Design Document)  
**산출물 ID:** MD06  
**모듈:** RETS-MODULE-03 — 요구사항 기반 규모 산정 및 투입 계획 도구  
**버전:** 1.0  
**작성일:** 2026-05-07  
**참고 문서:**
- `rets_module_03_03_srs.xlsx` (MD03 — 소프트웨어 요구사항 명세서)
- `rets_module_03_04_tdd.md` (MD04 — 테스트 시나리오-케이스 설계서)
- `rets_module_03_05_ddd.md` (MD05 — 데이터 설계서)
- `00_rfp_requirements/rets_02b_design_requirements.md` (RETS UI/UX 디자인 요구사항 v4)
- `00_rfp_requirements/rets_02b_design_system_html/02b_design_system_ref.html` (RETS 디자인 시스템 참조)

---

## 목차

1. [문서 개요](#1-문서-개요)
2. [화면 목록 및 네비게이션](#2-화면-목록-및-네비게이션)
3. [SCR-ALL: 공통 UI 요소](#3-scr-all-공통-ui-요소)
4. [SCR-01: 프로젝트 대시보드](#4-scr-01-프로젝트-대시보드)
5. [SCR-02: 요구사항 산정 그리드](#5-scr-02-요구사항-산정-그리드)
6. [SCR-03: 프로젝트 초기 설정 드로어](#6-scr-03-프로젝트-초기-설정-드로어)
7. [SCR-04: 간트 차트 일정 계획](#7-scr-04-간트-차트-일정-계획)
8. [공통 컴포넌트 상세 설계](#8-공통-컴포넌트-상세-설계)
9. [LLM 수락/거부 UX 패턴](#9-llm-수락거부-ux-패턴)
10. [반응형 레이아웃 처리](#10-반응형-레이아웃-처리)
11. [요구사항 추적성 매트릭스](#11-요구사항-추적성-매트릭스)

---

## 1. 문서 개요

### 1.1 목적

본 문서는 RETS-MODULE-03 단일 HTML 애플리케이션(`rets_module_03_07_app.html`)의 각 화면에 대한 레이아웃 구조, UI 컴포넌트 배치, 인터랙션 흐름, 상태 처리 방식을 정의한다. 개발팀은 본 설계서를 기준으로 구현을 진행하여야 하며, RETS 디자인 시스템(RFP-UI-REQ-01~05) 및 SRS 기능 요구사항과의 일치성을 확보하여야 한다.

### 1.2 범위

| 항목 | 내용 |
|------|------|
| 대상 파일 | `rets_module_03_07_app.html` (단일 HTML, Vanilla JS) |
| 화면 수 | 4개 탭 화면 + 공통 요소(SCR-ALL) |
| 언어 | 한국어/영어 동시 지원 (i18n 객체 기반) |
| 테마 | 라이트 모드 (기본) / 다크 모드 전환 |
| 기준 해상도 | 1280px 이상 (데스크톱 우선) |

### 1.3 설계 원칙

- **단일 HTML 제약 준수:** 모든 CSS, JS, SVG, 이미지(Base64)가 하나의 HTML 파일 안에 포함되어야 한다.
- **RETS 디자인 시스템 준수:** shadcn/ui 계열 미니멀 스타일, 포인트 컬러 `#F97316`, Noto Sans KR, Blur Glass 헤더.
- **팝업 금지:** `alert()`, `confirm()`, `prompt()` 절대 사용 금지. 대신 Toast(알림)/Modal(확인)/Drawer(상세 패널) 사용.
- **AI 자동 반영 금지:** LLM이 추천한 값은 사용자가 수락(Accept) / 거부(Reject)한 이후에만 반영.
- **비동기 UX 필수:** 파일 I/O, LLM 호출 등 1초 초과 가능 작업은 로딩 스피너 및 상태 피드백 제공.

### 1.4 화면 ID 체계

| 화면 ID | 화면명 | 탭 번호 |
|---------|--------|---------|
| SCR-ALL | 공통 UI 요소 (헤더, 토스트, 모달, 드로어) | — |
| SCR-01 | 프로젝트 대시보드 | Tab 1 |
| SCR-02 | 요구사항 산정 그리드 | Tab 2 |
| SCR-03 | 프로젝트 초기 설정 드로어 | Drawer (모든 탭에서 접근) |
| SCR-04 | 간트 차트 일정 계획 | Tab 3 |

---

## 2. 화면 목록 및 네비게이션

### 2.1 전체 화면 구조

```
┌────────────────────────────────────────────────────────────────────────┐
│  HEADER (SCR-ALL)                                                      │
│  [RETS 뱃지] [모듈명]       [조직 로고]       [◐] [⚙] [불러오기] [내보내기] │
└────────────────────────────────────────────────────────────────────────┘
┌──────────────┬───────────────────┬──────────────┐
│ 📊 대시보드   │ 📋 요구사항 산정  │ 📅 간트 차트  │
└──────────────┴───────────────────┴──────────────┘
┌────────────────────────────────────────────────────────────────────────┐
│                                                                        │
│   BODY (활성 탭의 내용 표시)                                            │
│                                                                        │
└────────────────────────────────────────────────────────────────────────┘
```

### 2.2 탭 네비게이션 규칙

- 탭은 3개로 고정: **대시보드 (SCR-01)**, **요구사항 산정 (SCR-02)**, **간트 차트 (SCR-04)**
- 초기 진입 시 **SCR-01 (대시보드)** 가 활성 탭으로 표시된다.
- JSON 파일 로드 전에는 탭 전환 가능하나, SCR-02·SCR-04는 "데이터 없음(Empty)" 상태를 표시한다.
- 활성 탭은 포인트 컬러(`#F97316`) 하단 밑줄 강조 + Bold 텍스트로 식별된다.
- 탭 전환 시 이전 탭 상태(스크롤 위치, 필터 등)는 JavaScript 상태 변수에 유지된다.

### 2.3 드로어 접근 경로

| 드로어 | 접근 방법 | 화면 ID |
|--------|-----------|---------|
| 프로젝트 초기 설정 | 헤더 우측 ⚙ 버튼 클릭 | SCR-03 |
| 요구사항 상세 편집 | SCR-02 행 클릭 또는 편집 아이콘 클릭 | SCR-02 인라인 드로어 |

---

## 3. SCR-ALL: 공통 UI 요소

### 3.1 헤더 (Header)

#### 3.1.1 레이아웃 구조

```
┌────────────────────────────────────────────────────────────────────────┐
│ ← flex: 1 →                    ← flex-shrink:0 →     ← flex: 1 →     │
│ [RETS] 규모 산정 및 투입 계획    [솔링크 로고 이미지]   [◐][⚙][불러오기][내보내기]│
│ ← 좌측 (모듈 정보)              ← 중앙 (조직 로고)      ← 우측 (버튼 그룹) │
└────────────────────────────────────────────────────────────────────────┘
```

#### 3.1.2 시각 스타일

| 속성 | 라이트 모드 | 다크 모드 |
|------|------------|----------|
| 배경색 (`--header-bg`) | `rgba(240, 242, 245, 0.72)` | `rgba(14, 35, 35, 0.80)` |
| backdrop-filter | `blur(14px) saturate(160%)` | 동일 |
| 하단 경계선 | `1px solid var(--border)` | 동일 |
| 높이 | `52px` | 동일 |
| 좌우 패딩 | `30px` | 동일 |
| position | `sticky; top: 0; z-index: 100` | 동일 |

#### 3.1.3 좌측 영역 — 모듈 정보

| 요소 | 내용 | 스타일 |
|------|------|--------|
| 모듈 뱃지 | `RETS` 텍스트 | 배경 `#C2560B`, 흰색 텍스트, 11px Bold, padding `2px 7px`, radius `4px` |
| 모듈명 | `규모 산정 및 투입 계획` (한글) / `Sizing & Planning` (영문) | 15px Bold, `var(--text-pri)` |

#### 3.1.4 중앙 영역 — 조직 로고

- 조직 로고 이미지: `logo_light.png` (라이트 모드) / `logo_dark.png` (다크 모드)
- CSS `[data-theme="dark"]` 선택자로 이미지 전환
- 높이: `28px`, 비율 유지 (`object-fit: contain`)
- Base64 인라인 임베드 권장 (단일 HTML 파일 제약)

#### 3.1.5 우측 영역 — 버튼 그룹

| 버튼 | 아이콘 | 레이블 (ko/en) | 동작 |
|------|--------|---------------|------|
| 테마 전환 | ◐ (반원 아이콘) | — | `data-theme` 토글 (`light` ↔ `dark`), `localStorage` 저장 |
| 프로젝트 설정 | ⚙ | — | SCR-03 드로어 열기 |
| 불러오기 | ↑ | `불러오기` / `Load` | 숨김 `<input type="file" accept=".json">` 트리거 |
| 내보내기 | ↓ | `내보내기` / `Export` | 산정 완료 JSON 다운로드 |

- 불러오기·내보내기 버튼: `ghost` 스타일 버튼 (테두리 있음, 배경 투명)
- 비활성 상태: 파일 미로드 시 내보내기 버튼 `disabled` (연회색 처리)

#### 3.1.6 탭 바 (Working Tabs)

```
┌──────────────┬─────────────────────┬──────────────┐
│ 📊 대시보드  │ 📋 요구사항 산정    │ 📅 간트 차트  │
│ ──────────── │                     │              │
│ (활성: 주황색 하단 밑줄 + Bold)                      │
└──────────────┴─────────────────────┴──────────────┘
```

| 속성 | 값 |
|------|-----|
| 탭 바 배경 | `var(--surface)` |
| 탭 바 하단 경계선 | `1px solid var(--border)` |
| 탭 높이 | `40px` |
| 좌우 패딩 | `30px` |
| 활성 탭 색상 | 포인트 컬러 `#F97316` 하단 `2px` 밑줄 + Bold |
| 비활성 탭 색상 | `var(--text-sec)`, hover 시 `var(--text-pri)` |
| 탭 텍스트 | 13px Medium (UI Label 스케일) |

---

### 3.2 Toast 알림

#### 3.2.1 위치 및 레이아웃

```
                                      ┌─────────────────────────────────┐
                                      │ [아이콘] 메시지 텍스트     [×] │  ← Toast #1
                                      ├─────────────────────────────────┤
                                      │ [아이콘] 메시지 텍스트     [×] │  ← Toast #2
                                      └─────────────────────────────────┘
                                             화면 우측 하단 고정
```

- `position: fixed; bottom: 24px; right: 24px; z-index: 9999`
- 최대 동시 표시 개수: **4개** (초과 시 가장 오래된 Toast 자동 제거)
- 너비: `min-content ~ 360px`

#### 3.2.2 Toast 유형

| 유형 | 아이콘 | 라이트 배경 | 라이트 보더 | 다크 배경 | 다크 보더 | 자동 닫힘 |
|------|--------|-----------|-----------|---------|---------|---------|
| `success` | ✓ (초록) | `#F0FDF4` | `#BBF7D0` | `#052E16` | `#166534` | 3초 |
| `error` | ✕ (빨강) | `#FEF2F2` | `#FECACA` | `#450A0A` | `#991B1B` | 유지 |
| `warning` | ⚠ (노랑) | `#FFFBEB` | `#FDE68A` | `#451A03` | `#92400E` | 5초 |
| `info` | ℹ (파랑) | `#EFF6FF` | `#BFDBFE` | `#172554` | `#1E40AF` | 3초 |
| `action` | — | `var(--surface)` | `var(--border-md)` | 동일 | 동일 | 수동 |

- **action 타입 (버튼 포함):** 사용자가 버튼을 클릭하거나 ×로 닫기 전까지 자동으로 사라지지 않는다.
- **중복 방지:** 동일 `toastKey`의 Toast가 이미 표시 중이면 기존 것을 교체한다.
- 닫기(×) 버튼: 테두리 없이 아이콘만 표시 (`12px`, `var(--text-sec)`)

#### 3.2.3 주요 Toast 발생 시나리오

| 시나리오 | 유형 | 메시지 예시 (ko) |
|---------|------|----------------|
| JSON 파일 로드 성공 | success | `요구사항 N개가 로드되었습니다.` |
| JSON 파일 파싱 실패 | error | `JSON 파일 형식이 올바르지 않습니다.` |
| LLM 스토리포인트 추천 완료 | action | `스토리포인트 추천 완료. 수락 또는 거부하세요.` [수락 전체] |
| LLM 호출 실패 | error | `LLM 서버 연결에 실패하였습니다. 다시 시도하세요.` |
| 내보내기 성공 | success | `요구사항 산정 결과가 내보내기 되었습니다.` |
| 컬러 코드 복사 | success | `#F97316 복사되었습니다.` |
| 설정 저장 | success | `프로젝트 설정이 저장되었습니다.` |

---

### 3.3 공통 로딩 오버레이

- LLM 호출 또는 대용량 파일 파싱 시 화면 전체에 반투명 오버레이 + 스피너 표시
- `position: fixed; inset: 0; background: rgba(0,0,0,0.35); z-index: 500`
- 스피너: 포인트 컬러 원형 회전 애니메이션 (`border-color: #F97316 transparent transparent transparent`)
- 로딩 메시지: 12px Caption 텍스트 (예: `스토리포인트 추천 중...`)

---

## 4. SCR-01: 프로젝트 대시보드

### 4.1 화면 개요

| 항목 | 내용 |
|------|------|
| 화면 ID | SCR-01 |
| 화면명 | 프로젝트 대시보드 |
| 탭 번호 | Tab 1 (기본 진입 화면) |
| 레이아웃 | 1-column (단일 컬럼 전체 너비) |
| 관련 SRS | DEV_FR_008_02, DEV_NFR_001, DEV_NFR_002 |
| 목적 | 프로젝트 전체 현황, 진행률, 리스크를 한눈에 파악하는 요약 화면 |

### 4.2 와이어프레임

```
┌────────────────────────────────────────────────────────────────────────┐
│ HEADER (SCR-ALL)                                                       │
├────────────────────────────────────────────────────────────────────────┤
│ [📊 대시보드 ──]  [📋 요구사항 산정]  [📅 간트 차트]                   │
├────────────────────────────────────────────────────────────────────────┤
│                                                                        │
│  ← 30px padding →                                          ← 30px →   │
│  ┌──────────────────────────────────────────────────────────────────┐  │
│  │ 🏗 프로젝트명                          팀: [TEAM_ID]            │  │
│  │ [기한: 2026-05-31]  [총 공수: 120 MD]  [산정 기준: 0.5 MD/SP]   │  │
│  └──────────────────────────────────────────────────────────────────┘  │
│                                                                        │
│  ┌──────────────┐ ┌──────────────┐ ┌──────────────┐ ┌──────────────┐  │
│  │ 전체 요구사항  │ │ 산정 완료    │ │ 총 스토리포인트│ │ 예상 총 공수  │  │
│  │  [숫자]       │ │ [숫자] / [N] │ │   [숫자] SP  │ │   [숫자] MD  │  │
│  │ 개             │ │  [진행률 바]  │ │              │ │              │  │
│  └──────────────┘ └──────────────┘ └──────────────┘ └──────────────┘  │
│                                                                        │
│  ┌─────────────────────────────────────────────────────┐              │
│  │ 📊 중요도 분포                                        │              │
│  │  Critical ████████ 3       High ████████████ 8       │              │
│  │  Medium   ██████   4       Low  ███          2       │              │
│  └─────────────────────────────────────────────────────┘              │
│                                                                        │
│  ┌─────────────────────────────────────┐ ┌────────────────────────┐   │
│  │ 📋 MoSCoW 우선순위 분포              │ │ ⚠ 리스크 항목           │   │
│  │  MUST   ███████████████ 12          │ │ ─────────────────────── │   │
│  │  SHOULD █████████      7           │ │ [!] 의존관계 미설정 3건  │   │
│  │  COULD  ████           3           │ │ [!] SP 미산정 8건        │   │
│  │  WONT   ██             1           │ │ [!] 역할 미배정 5건      │   │
│  └─────────────────────────────────────┘ └────────────────────────┘   │
│                                                                        │
│  ┌──────────────────────────────────────────────────────────────────┐  │
│  │ 📋 역할별 공수 배분                                               │  │
│  │  PM            ██████████  12.5 MD  │ 풀스택 개발  ██████  42 MD │  │
│  │  UI/UX 개발    ████████    18.0 MD  │ QA           ████   15 MD  │  │
│  └──────────────────────────────────────────────────────────────────┘  │
│                                                                        │
│  ┌──────────────────────────────────────────────────────────────────┐  │
│  │ 📅 일정 현황 미리보기                                             │  │
│  │  남은 기간: 24일  │  완료 예정: [날짜]  │  일정 여유: [일수]일   │  │
│  └──────────────────────────────────────────────────────────────────┘  │
│                                                                        │
└────────────────────────────────────────────────────────────────────────┘
```

### 4.3 구성 요소 상세

#### 4.3.1 프로젝트 정보 배너

| 요소 | 내용 | 비고 |
|------|------|------|
| 프로젝트명 | `ProjectConfig.name` 값 표시 | 미설정 시 `"프로젝트명 미설정"` 플레이스홀더 |
| 팀 ID | `AppState.team_id` 값 (Badge 형태) | LLM Relay 세션 연동 |
| 기한 | `ProjectConfig.deadline` | `YYYY-MM-DD` 형식 |
| 총 공수 | `ProjectConfig.totalMD` + `MD` 단위 | — |
| 산정 기준 | `ProjectConfig.mdPerSp` + `MD/SP` | 기본값 `0.5` |
| 프로젝트 설정 버튼 | ⚙ 아이콘 | 클릭 시 SCR-03 드로어 열기 |

#### 4.3.2 KPI 카드 (4개)

| 카드 | 데이터 소스 | 설명 |
|------|------------|------|
| 전체 요구사항 | `requirements.length` | 로드된 전체 DEV REQ 수 |
| 산정 완료 | SP > 0인 요구사항 수 / 전체 | 진행률 바 포함 (`var(--brand-500)`) |
| 총 스토리포인트 | `Σ requirement.story_points` | 미산정 요구사항 제외 |
| 예상 총 공수 | `Σ requirement.estimated_md` | `MD` 단위 |

- KPI 카드 레이아웃: `display: grid; grid-template-columns: repeat(4, 1fr); gap: 12px`
- 카드 배경: `var(--surface)`, 테두리: `1px solid var(--border)`, 반경: `8px`
- 숫자 폰트: 20px Bold (Title 스케일 최대치)
- Empty 상태: 숫자 대신 `—` 표시, 설명 텍스트로 "JSON 파일을 불러오세요" 안내

#### 4.3.3 중요도 분포 차트

- CSS 가로 바 차트 (외부 라이브러리 없이 div 너비 % 제어)
- 중요도 유형: `Critical` (빨강), `High` (주황), `Medium` (노랑), `Low` (초록)
- 각 바: 레이블 + 시각적 바 + 숫자 카운트

#### 4.3.4 MoSCoW 우선순위 분포

- CSS 가로 바 차트 (중요도 분포와 동일 패턴)
- `MUST` (포인트 컬러), `SHOULD` (파랑), `COULD` (연회색), `WONT` (비활성 회색)

#### 4.3.5 리스크 항목 패널

- Warning 타입 상태 표시 카드 (배경: `var(--warning-50)`, 테두리: `var(--warning-200)`)
- 표시 항목:
  - SP 미산정 요구사항 수
  - 역할 미배정 요구사항 수
  - 의존관계 순환 참조 감지 여부
  - 총 예상 공수 > 총 투입 가능 공수 여부 (초과 시 Error 타입으로 강조)
- 리스크 없음 시: Success 타입 카드 "모든 항목이 산정 완료되었습니다."

#### 4.3.6 역할별 공수 배분 패널

- 인력 구성(`PersonnelConfig.items`)별 할당된 총 공수(`estimated_md`) 합산
- CSS 가로 바 차트로 비교 시각화
- 총 투입 가능 공수 대비 초과 여부를 색상으로 구분 (초과 시 Error 색상)

#### 4.3.7 일정 현황 미리보기

| 요소 | 계산 방식 |
|------|---------|
| 남은 기간 | `ProjectConfig.deadline` - 오늘 날짜 (일 수) |
| 완료 예정일 | `GanttPlan`의 마지막 작업 종료일 |
| 일정 여유 | 마감일 - 완료 예정일 (음수 시 Error 강조) |

### 4.4 상태별 처리

| 상태 | 표시 방법 |
|------|---------|
| Empty (파일 미로드) | 전체 카드에 `—` 또는 `0` 표시; "JSON 파일을 불러오세요" 안내 배너 표시 |
| 데이터 있음 | 계산된 실제 값 표시 |
| 계산 오류 | 해당 카드에 Warning 스타일 + 오류 메시지 |

---

## 5. SCR-02: 요구사항 산정 그리드

### 5.1 화면 개요

| 항목 | 내용 |
|------|------|
| 화면 ID | SCR-02 |
| 화면명 | 요구사항 산정 그리드 |
| 탭 번호 | Tab 2 |
| 레이아웃 | 2-column (좌: 그리드, 우: AI 보조 패널) + 드래그 리사이저 |
| 관련 SRS | DEV_FR_001, DEV_FR_002, DEV_FR_004, DEV_FR_005, DEV_FR_006, DEV_FR_007 |
| 목적 | 요구사항별 산정 (SP, 역할, 공수), 의존관계 설정, AI 추천 수락/거부 |

### 5.2 와이어프레임 (2-column 레이아웃)

```
┌────────────────────────────────────────────────────────────────────────┐
│ HEADER + TABS (SCR-ALL)                                                │
├────────────────────────────────────────────────────────────────────────┤
│                                                                        │
│ [🤖 AI 전체 SP 추천]  [🤖 AI 전체 역할 추천]   [필터▼] [검색🔍]        │
│                                                                        │
├──────────────────────────────────┊─────────────────────────────────────┤
│ ← 좌측 컬럼 (기본 60%)           ┊  ← 우측 컬럼 (기본 40%)             │
│                                  ┊                                     │
│ ┌──────────────────────────────┐ ┊ ┌─────────────────────────────────┐ │
│ │ 요구사항 산정 그리드          │ ┊ │ AI 추천 보조 패널                │ │
│ │──────────────────────────────│ ┊ │─────────────────────────────────│ │
│ │ ID  │이름│중요│우선│SP│역할  │ ┊ │ [추천 대기 중...]               │ │
│ │─────┼────┼────┼────┼──┼─────│ ┊ │                                 │ │
│ │FR001│로드│High│MUST│ 3│풀스택│ ┊ │ ──────────────────────────────  │ │
│ │  001│파싱│    │    │  │     │ ┊ │ DEV_FR_001                      │ │
│ │─────┼────┼────┼────┼──┼─────│ ┊ │ 추천 SP: 3 ★                   │ │
│ │FR002│그리│Crit│MUST│ 5│풀스택│ ┊ │ 근거: "요구사항 파일 파싱은..."  │ │
│ │  002│드뷰│    │    │  │ UI  │ ┊ │ [✓ 수락] [✕ 거부]              │ │
│ │─────┼────┼────┼────┼──┼─────│ ┊ │                                 │ │
│ │ ...  │    │    │    │  │     │ ┊ │ DEV_FR_002                      │ │
│ └──────────────────────────────┘ ┊ │ 추천 SP: 5 ★                   │ │
│                                  ┊ │ 근거: "그리드 CRUD는..."        │ │
│                                  ┊ │ [✓ 수락] [✕ 거부]              │ │
│                                  ┊ └─────────────────────────────────┘ │
└────────────────────────────────────────────────────────────────────────┘
                       ↑ 드래그 리사이저 (포인트 컬러 hover)
```

### 5.3 구성 요소 상세

#### 5.3.1 상단 액션 바

| 버튼/컨트롤 | 레이블 (ko/en) | 동작 |
|------------|--------------|------|
| AI 전체 SP 추천 | `🤖 SP 일괄 추천` / `Batch SP Suggest` | 로드된 모든 요구사항의 SP를 LLM에게 일괄 추천 요청 |
| AI 전체 역할 추천 | `🤖 역할 일괄 추천` / `Batch Role Suggest` | 모든 요구사항의 역할/공수를 LLM에게 일괄 추천 요청 |
| 필터 드롭다운 | `필터` / `Filter` | 중요도/우선순위/산정상태별 필터 팝오버 (Popover) |
| 검색 인풋 | `검색...` / `Search...` | 요구사항 ID 또는 이름으로 실시간 필터 |

- AI 추천 버튼은 LLM 세션 미초기화 상태에서 `disabled` 처리 + 툴팁 "프로젝트 설정에서 팀 ID를 입력하세요"

#### 5.3.2 요구사항 산정 그리드 (좌측 컬럼)

**컬럼 정의:**

| # | 컬럼명 | 너비 | 내용 | 편집 가능 |
|---|--------|------|------|----------|
| 1 | 요구사항 ID | 120px | `DEV_FR_NNN` 형식, `<code>` 태그 스타일 | 불가 (읽기 전용) |
| 2 | 요구사항명 | flex | 이름 텍스트; 들여쓰기로 계층 표현 (level에 따라 `padding-left`) | 클릭 시 드로어 편집 |
| 3 | 중요도 | 90px | `Critical/High/Medium/Low` 배지 | 셀 클릭 시 드롭다운 |
| 4 | 우선순위 | 80px | `MUST/SHOULD/COULD/WONT` 배지 | 셀 클릭 시 드롭다운 |
| 5 | SP | 60px | 피보나치 숫자 또는 `—`; AI 추천 대기 중 스피너 | 셀 클릭 시 숫자 드롭다운 |
| 6 | 역할 | 100px | 담당 역할 축약 텍스트 또는 `—` | 셀 클릭 시 드롭다운 |
| 7 | 공수 (MD) | 70px | `estimated_md` 수치 또는 `—` | 자동 계산 (SP × mdPerSp) |
| 8 | 의존관계 | 80px | 선행 ID 수 (예: `↑2개`) | 클릭 시 드로어 |
| 9 | 액션 | 60px | 편집(✏) 아이콘 | 클릭 시 편집 드로어 |

**그리드 시각 규칙:**

| 규칙 | 세부 내용 |
|------|---------|
| 계층 들여쓰기 | `req_level 1`: 0px / `level 2`: 16px / `level 3`: 32px / `level 4`: 48px / `level 5`: 64px |
| LLM 추천 대기 행 | `_sp_pending: true`인 행: 배경 `var(--brand-50)` + 좌측 `3px solid var(--brand-500)` 테두리 |
| SP 미산정 행 | SP 열 배경 `var(--warning-50)` |
| 선택 행 | 배경 `var(--surface-2)` + 포인트 컬러 좌측 테두리 `3px` |
| 호버 행 | 배경 `var(--surface)` |
| 헤더 행 | `var(--surface-2)` 배경, 12px Bold, `var(--text-sec)` |

**중요도 배지 색상:**

| 중요도 | 배지 배경 (라이트) | 배지 텍스트 |
|--------|-----------------|-----------|
| Critical | `#FEF2F2` | `#8c3131` |
| High | `#FFF7ED` | `#C2560B` |
| Medium | `#FFFBEB` | `#D97706` |
| Low | `#F0FDF4` | `#1CB357` |

**우선순위 배지 색상:**

| 우선순위 | 배지 배경 (라이트) | 배지 텍스트 |
|---------|-----------------|-----------|
| MUST | `#FFF7ED` | `#F97316` |
| SHOULD | `#EFF6FF` | `#3375E8` |
| COULD | `var(--surface-2)` | `var(--text-sec)` |
| WONT | `var(--surface-2)` | `var(--text-dis)` |

**SP 드롭다운 옵션:**  
`—` (미산정), `1`, `2`, `3`, `5`, `8`, `13`, `21` (피보나치 수열)

**그리드 상태 처리:**

| 상태 | 표시 |
|------|------|
| Empty (파일 미로드) | 빈 그리드 + 중앙 "JSON 파일을 불러오세요" 안내 + 불러오기 버튼 |
| Loading | 행 스켈레톤 애니메이션 |
| Error | Error 배너 + 오류 메시지 |

#### 5.3.3 AI 추천 보조 패널 (우측 컬럼)

| 상태 | 표시 내용 |
|------|---------|
| 초기 (추천 없음) | "AI 추천 버튼을 클릭하면 스토리포인트와 역할을 자동 추천합니다." 안내 |
| LLM 호출 중 | 스피너 + "추천 생성 중..." |
| 추천 완료 | 요구사항별 추천 카드 목록 |
| 모두 처리됨 | "모든 추천 항목이 처리되었습니다." |

**추천 카드 구조 (한 요구사항당):**

```
┌─────────────────────────────────────────────┐
│ [DEV_FR_001_01] 불러오기 파싱               │  ← 요구사항 ID + 이름
│                                             │
│  SP 추천: ● 3                               │  ← 피보나치 숫자, 포인트 컬러
│  역할: 풀스택 개발자                         │  ← 배정 역할
│  예상 공수: 1.5 MD                          │  ← SP × mdPerSp
│                                             │
│  추천 근거:                                  │
│  "JSON 파싱은 파일 읽기 및 유효성            │  ← LLM reasoning (최대 2줄, 말줄임)
│   검증을 포함하므로..."                      │
│                                             │
│  [✓ 수락]          [✕ 거부]               │  ← 버튼 그룹
└─────────────────────────────────────────────┘
```

- **수락(Accept):** 카드의 값을 그리드 행에 반영하고 카드 제거, success Toast
- **거부(Reject):** 카드만 제거 (기존 값 유지), `_sp_pending: false`

**일괄 수락/거부 버튼:**  
패널 상단에 `[전체 수락]` / `[전체 거부]` 버튼 제공 (3개 이상 카드가 있는 경우 표시)

#### 5.3.4 드래그 리사이저

- 좌·우 컬럼 사이 `8px` 너비 영역
- 호버 시: 포인트 컬러(`#F97316`) + 세로 점선 또는 컬러 바 표시
- 드래그 중: 포인트 컬러 유지 + 커서 `col-resize`
- 각 컬럼 최소 너비: `160px` (RFP-UI-REQ-02 준수)
- `mousedown` → `mousemove` → `mouseup` 이벤트로 동작

#### 5.3.5 의존관계 설정

- 의존관계는 그리드 열의 아이콘 클릭 시 **의존관계 편집 모달**에서 설정한다.
- 모달 내 "선행 요구사항 선택" 멀티 셀렉트 드롭다운 제공
- 순환 참조(Cyclic Dependency) 감지 시: Error Toast + 선택 불가 처리
- 의존 요구사항이 설정된 행: 의존관계 열에 `↑N개` 배지 (N = 선행 요구사항 수)

---

## 6. SCR-03: 프로젝트 초기 설정 드로어

### 6.1 화면 개요

| 항목 | 내용 |
|------|------|
| 화면 ID | SCR-03 |
| 화면명 | 프로젝트 초기 설정 드로어 |
| UI 패턴 | Drawer (화면 우측 슬라이드인) |
| 트리거 | 헤더 ⚙ 버튼, SCR-01 설정 버튼 |
| 관련 SRS | DEV_FR_003, DEV_NFR_001, DEV_NFR_002 |
| 목적 | 프로젝트 기본 정보, 기한, 총 공수, 인력 구성, LLM 팀 ID 설정 |

### 6.2 와이어프레임

```
┌───────────────────────────────────────────────┐ ← max-width: 400px; max: 90vw
│  ┌─────────────────────────────────────────┐  │ ← Header (고정)
│  │ 프로젝트 초기 설정             [×]     │  │
│  └─────────────────────────────────────────┘  │
│                                               │
│  (이하 Body — 독립 스크롤)                    │
│  ┌─────────────────────────────────────────┐  │
│  │ 기본 정보                               │  │ ← 섹션 헤딩 (14px Bold)
│  │                                         │  │
│  │ 프로젝트명 *                            │  │ ← Label
│  │ ┌─────────────────────────────────────┐ │  │
│  │ │ 프로젝트명을 입력하세요...           │ │  │ ← Input (text)
│  │ └─────────────────────────────────────┘ │  │
│  │                                         │  │
│  │ 팀 ID (LLM 세션) *                     │  │
│  │ ┌─────────────────────────────────────┐ │  │
│  │ │ 팀 ID를 입력하세요...               │ │  │ ← Input (text)
│  │ └─────────────────────────────────────┘ │  │
│  │ ℹ LLM 릴레이 서버 세션 ID로 사용됩니다   │  │ ← help text (caption)
│  └─────────────────────────────────────────┘  │
│                                               │
│  ┌─────────────────────────────────────────┐  │
│  │ 일정 및 공수                            │  │
│  │                                         │  │
│  │ 프로젝트 마감일 *                       │  │
│  │ ┌─────────────────────────────────────┐ │  │
│  │ │ 2026-05-31                          │ │  │ ← Input (date)
│  │ └─────────────────────────────────────┘ │  │
│  │                                         │  │
│  │ 총 투입 가능 공수 (MD) *               │  │
│  │ ┌────────────────┐                     │  │
│  │ │ 120            │ MD                  │  │ ← Input (number) + unit label
│  │ └────────────────┘                     │  │
│  │                                         │  │
│  │ MD/SP 산정 계수 *                      │  │
│  │ ┌────────────────┐                     │  │
│  │ │ 0.5            │ MD/SP               │  │ ← Input (number, step: 0.1)
│  │ └────────────────┘                     │  │
│  │ ℹ 스토리포인트 1당 투입 공수 기본값      │  │
│  └─────────────────────────────────────────┘  │
│                                               │
│  ┌─────────────────────────────────────────┐  │
│  │ 인력 구성                   [+ 추가]    │  │
│  │                                         │  │
│  │ ┌──────────────────────────┬─────┬───┐  │  │
│  │ │ 역할명                   │ 인원 │ 삭 │  │  │
│  │ ├──────────────────────────┼─────┼───┤  │  │
│  │ │ PM / 기획 리드            │  1  │ × │  │  │
│  │ │ 풀스택 개발자 (Frontend)  │  1  │ × │  │  │
│  │ │ 개발자 (UI/UX)           │  1  │ × │  │  │
│  │ │ QA / 테스트              │  1  │ × │  │  │
│  │ │ 문서 담당                │  1  │ × │  │  │
│  │ └──────────────────────────┴─────┴───┘  │  │
│  │                                         │  │
│  │ 총 가용 공수: 120 MD (자동 계산)        │  │ ← read-only 표시 필드
│  └─────────────────────────────────────────┘  │
│                                               │
│  ┌─────────────────────────────────────────┐  │ ← Footer (고정, border-top)
│  │          [취소]      [저장]             │  │
│  └─────────────────────────────────────────┘  │
└───────────────────────────────────────────────┘
```

### 6.3 드로어 동작 규칙

| 규칙 | 상세 |
|------|------|
| 진입 애니메이션 | 우측에서 `translateX(100%)` → `translateX(0)`, `0.25s ease` |
| 닫기 방법 | ① ×버튼 클릭 ② 배경 오버레이 클릭 ③ 취소 버튼 클릭 |
| 배경 오버레이 | `rgba(0,0,0,0.45)` 전체 화면 |
| 배경 스크롤 | 드로어 열림 동안 `body: overflow: hidden` |
| Body 스크롤 | 콘텐츠 초과 시 드로어 Body 독립 스크롤 |
| 왼쪽 경계선 | `border-left: 1px solid var(--border)` |
| 비저장 변경 | 닫기 시도 시 변경 사항이 있으면 Modal 확인 "변경 사항을 저장하지 않고 닫으시겠습니까?" |

### 6.4 필드 상세 및 유효성 검증

| 필드 | 타입 | 필수 | 기본값 | 유효성 규칙 |
|------|------|------|--------|------------|
| 프로젝트명 | text | ✓ | — | 최소 1자, 최대 100자 |
| 팀 ID | text | ✓ | — | 최소 1자, LLM 세션 ID 형식 |
| 마감일 | date | ✓ | — | 오늘 이후 날짜 |
| 총 투입 공수 | number | ✓ | `120` | 1 이상 정수 |
| MD/SP 계수 | number | ✓ | `0.5` | 0.1 이상, 0.1 단위 |
| 역할명 | text | ✓ (1개 이상) | 기본 5개 | 최소 1자 |
| 인원 수 | number | ✓ | `1` | 1 이상 정수 |

- 유효성 오류 시: 해당 필드 테두리 `var(--error-500)`, 하단 오류 메시지 `12px` Caption
- 저장 버튼 클릭 시 전체 유효성 검사 후 오류 있으면 저장 차단
- 저장 성공 시: 드로어 닫힘 + success Toast "프로젝트 설정이 저장되었습니다."
- `localStorage`에 `rets_m03_project_config`, `rets_m03_personnel_config` 키로 저장

### 6.5 총 가용 공수 자동 계산

```
총 가용 공수 (MD) = Σ (역할별 인원 수 × 일일 근무시간(8h) / 8 × 총 근무일 수)
총 근무일 수 = 오늘 ~ 마감일 사이의 평일 수 (주말 제외)
```

- 계산 결과는 드로어 하단에 `read-only` 텍스트로 실시간 표시
- `총 투입 가능 공수` 입력 필드 값과 비교하여 차이가 10% 이상이면 Warning 메시지 표시

---

## 7. SCR-04: 간트 차트 일정 계획

### 7.1 화면 개요

| 항목 | 내용 |
|------|------|
| 화면 ID | SCR-04 |
| 화면명 | 간트 차트 일정 계획 |
| 탭 번호 | Tab 3 |
| 레이아웃 | 2-column (좌: 제어 패널, 우: 간트 차트 영역) + 드래그 리사이저 |
| 관련 SRS | DEV_FR_007, DEV_FR_008_01, DEV_NFR_001, DEV_NFR_002 |
| 목적 | 의존관계 기반 주차별 일정 시각화 (Mermaid.js 간트 차트) |

### 7.2 와이어프레임

```
┌────────────────────────────────────────────────────────────────────────┐
│ HEADER + TABS (SCR-ALL)                                                │
├────────────────────────────────────────────────────────────────────────┤
│                                                                        │
│ [🔄 일정 재산정]  기준일: [오늘날짜]  표시: [주별▼] [역할별▼]          │
│                                                                        │
├──────────────────────┊─────────────────────────────────────────────────┤
│ ← 좌측 (기본 30%)    ┊  ← 우측 (기본 70%)                              │
│                      ┊                                                 │
│ ┌──────────────────┐ ┊ ┌───────────────────────────────────────────┐   │
│ │ 일정 요약 정보    │ ┊ │ Mermaid Gantt Chart                      │   │
│ │                  │ ┊ │                                           │   │
│ │ 총 기간:         │ ┊ │ gantt                                     │   │
│ │  [N]주           │ ┊ │   dateFormat YYYY-MM-DD                   │   │
│ │ 시작일:          │ ┊ │   axisFormat %m/%d                        │   │
│ │  2026-05-18      │ ┊ │   section MUST 요구사항                   │   │
│ │ 완료 예정:       │ ┊ │     로드 파싱:done, 05-18, 1d             │   │
│ │  2026-05-25      │ ┊ │     그리드 뷰 :active, 05-18, 3d         │   │
│ │ 일정 여유:       │ ┊ │   section SHOULD 요구사항                 │   │
│ │  +6일            │ ┊ │     ...                                   │   │
│ │                  │ ┊ │                                           │   │
│ │ ──────────────   │ ┊ │ (Mermaid 렌더링 결과 표시)               │   │
│ │ 역할별 요약      │ ┊ │                                           │   │
│ │ PM:    1.5MD    │ ┊ └───────────────────────────────────────────┘   │
│ │ 풀스택:42.0MD   │ ┊                                                 │
│ │ ...             │ ┊                                                 │
│ │                  │ ┊                                                 │
│ └──────────────────┘ ┊                                                 │
└────────────────────────────────────────────────────────────────────────┘
                       ↑ 드래그 리사이저
```

### 7.3 구성 요소 상세

#### 7.3.1 상단 제어 바

| 요소 | 동작 |
|------|------|
| `🔄 일정 재산정` 버튼 | 의존관계 위상정렬(Kahn's algorithm) 재실행 → Mermaid 구문 재생성 → 차트 재렌더링 |
| 기준일 표시 | `오늘 날짜` 읽기 전용 표시 |
| 표시 단위 드롭다운 | `주별 (Week)` / `일별 (Day)` — Mermaid `axisFormat` 변경 |
| 역할별 필터 드롭다운 | 특정 역할만 표시 필터 |

#### 7.3.2 좌측 컬럼 — 일정 요약 패널

| 정보 | 계산 방식 |
|------|---------|
| 총 기간 | 첫 작업 시작일 ~ 마지막 작업 종료일 (주 수 환산) |
| 시작일 | 위상정렬 후 첫 번째 작업의 `startDate` |
| 완료 예정일 | 모든 작업 종료 후 최종 `endDate` |
| 일정 여유 | `ProjectConfig.deadline` - 완료 예정일 |
| 일정 여유 표시 | 양수: success 색상 `+N일` / 음수: error 색상 `-N일` |

**역할별 공수 요약 테이블:**

| 컬럼 | 내용 |
|------|------|
| 역할명 | `PersonnelConfig.items[].role` |
| 배정 MD | 해당 역할에 배정된 요구사항의 `estimated_md` 합산 |
| 진행률 바 | 배정 MD / 총 가용 MD (초과 시 Error 색상) |

#### 7.3.3 우측 컬럼 — Mermaid 간트 차트

**Mermaid.js 렌더링 방식:**
- CDN: `https://cdn.jsdelivr.net/npm/mermaid@10/dist/mermaid.min.js`
- 렌더링 타겟: `<div class="mermaid">...</div>` 동적 생성
- `mermaid.init()` 호출로 차트 렌더링

**Mermaid 간트 구문 생성 알고리즘:**

```
1. 위상정렬 (Kahn's Algorithm):
   - 각 요구사항을 노드로 설정
   - predecessor_ids를 간선(edge)으로 설정
   - 진입차수(in-degree) 0인 노드부터 큐에 추가
   - BFS로 처리 순서 결정
   - 순환 감지 시 Error Toast 및 처리 중단

2. 시작/종료일 계산:
   - 시작일 = max(선행 요구사항 종료일) or 프로젝트 시작일
   - 소요일 = ceil(estimated_md / (일일근무 8h/8)) = ceil(estimated_md) 일
   - 종료일 = 시작일 + 소요일 (주말 제외)

3. Mermaid 구문 생성:
   gantt
     dateFormat  YYYY-MM-DD
     axisFormat  %m/%d
     section MUST
       [ID] : [status], [startDate], [N]d
     section SHOULD
       ...
```

**Mermaid 상태(status) 매핑:**

| 조건 | Mermaid 상태 |
|------|-------------|
| 완료 처리 (미래 기능) | `done` |
| 오늘 날짜 포함 기간 | `active` |
| 미래 | (상태 없음) |
| CRIT 마크 | `crit` (Critical 중요도 요구사항) |

**차트 상태 처리:**

| 상태 | 표시 |
|------|------|
| Empty (SP 미산정 요구사항 있음) | Warning 배너 "SP가 산정되지 않은 요구사항이 있습니다. 요구사항 산정 탭에서 먼저 완료하세요." |
| 순환 의존 감지 | Error 배너 + 해당 요구사항 ID 목록 표시 |
| 렌더링 완료 | Mermaid SVG 차트 정상 표시 |
| Mermaid 로드 실패 | Error Toast "차트 라이브러리 로드에 실패하였습니다." + 원시 Mermaid 텍스트 표시 |

#### 7.3.4 Mermaid 차트 스타일 커스터마이징

```javascript
mermaid.initialize({
  theme: document.documentElement.getAttribute('data-theme') === 'dark' ? 'dark' : 'default',
  gantt: {
    fontSize: 12,
    fontFamily: 'Noto Sans KR, sans-serif',
    barHeight: 20,
    barGap: 4,
    topPadding: 50,
    gridLineStartPadding: 35,
    numberSectionStyles: 4
  }
});
```

- 다크 모드 전환 시 Mermaid `theme: 'dark'` 재초기화 및 차트 재렌더링

---

## 8. 공통 컴포넌트 상세 설계

### 8.1 모달 (Modal)

**사용 시나리오:**
- 의존관계 설정 (요구사항 선행 요건 선택)
- 되돌릴 수 없는 작업 확인 (JSON 덮어쓰기 등)
- 간단한 설정 변경 (1~3개 필드)

**레이아웃:**

```
┌────────────────────────────────────┐
│ 모달 제목                   [×]   │ ← Header
├────────────────────────────────────┤
│                                    │
│   모달 본문 내용                    │ ← Body
│                                    │
├────────────────────────────────────┤
│              [취소]    [확인]      │ ← Footer
└────────────────────────────────────┘
```

| 속성 | 값 |
|------|-----|
| 최대 너비 | `460px` |
| 배경 오버레이 | `rgba(0,0,0,0.45)` |
| 열기 애니메이션 | `opacity: 0 → 1`, `scale(0.97) → scale(1)`, `0.15s ease` |
| 영역 간 구분선 | 사용 금지 (Header-Body, Body-Footer 구분선 없음) |
| 닫기 버튼 | 테두리 없이 아이콘(×)만 |
| 오버레이 클릭 | 닫기 지원 |

### 8.2 드로어 공통 규칙

| 규칙 | 상세 |
|------|------|
| 위치 | 화면 우측 (`position: fixed; right: 0; top: 0; height: 100%`) |
| 최대 너비 | `400px (max 90vw)` |
| 진입 애니메이션 | `translateX(100%) → translateX(0)`, `0.25s ease` |
| 왼쪽 경계 | `border-left: 1px solid var(--border)` |
| Header | 고정 높이 `52px`, 제목 + 닫기 버튼 |
| Body | 독립 스크롤 (`overflow-y: auto`) |
| Footer | 고정, 상단 `border-top: 1px solid var(--border)`, 저장/취소 버튼 |

### 8.3 버튼 스타일 체계

| 변형 (Variant) | 배경 | 텍스트 | 테두리 | 용도 |
|---------------|------|--------|--------|------|
| `primary` | `#F97316` | 흰색 | 없음 | 주요 액션 (저장, 수락) |
| `ghost` | 투명 | `var(--text-pri)` | `var(--border-md)` | 보조 액션 (불러오기, 내보내기) |
| `destructive` | `#FEF2F2` | `#8c3131` | `#FECACA` | 거부, 삭제 확인 |
| `secondary` | `var(--surface-2)` | `var(--text-pri)` | `var(--border)` | 취소, 닫기 |
| `link` | 투명 | `#F97316` | 없음 | 링크 스타일 부가 액션 |

- 호버: `filter: brightness(0.93)` 적용
- `disabled` 상태: `opacity: 0.45`, `cursor: not-allowed`
- 공통 `border-radius`: `6px (radius-md)`
- 공통 패딩: `8px 16px` (UI Label 13px 기준)

### 8.4 입력 필드 상태 표현

| 상태 | 테두리 색 | 포커스 외곽 | 배경 |
|------|---------|-----------|------|
| Default | `#D1D5DB` | — | `var(--bg)` |
| Focus | `#F97316` | `box-shadow: 0 0 0 3px var(--brand-50)` | `var(--bg)` |
| Error | `#8c3131` | `box-shadow: 0 0 0 3px #FEF2F2` | `#FEF2F2` |
| Warning | `#451A03` | — | `#FFFBEB` |
| Disabled | `#E5E7EB` | — | `var(--surface-2)`, `cursor: not-allowed` |
| Read-only | `#E5E7EB` | — | `var(--surface)` |

### 8.5 드롭다운 / 셀렉트

- 커스텀 드롭다운 (`<div>` 기반, `<select>` 사용 불가)
- `z-index: 200` 팝오버 레이어로 표시
- 선택된 항목: 체크 아이콘 + 포인트 컬러 텍스트
- 호버: `var(--surface)` 배경
- 키보드 탐색 지원 (화살표, Enter, Escape)

---

## 9. LLM 수락/거부 UX 패턴

### 9.1 흐름 다이어그램

```
사용자 클릭                  LLM 호출                   추천 결과
[AI SP 추천] ──→ [로딩 스피너] ──→ [추천 카드 목록 표시] ──→ 사용자 결정
     │                                                       │
     │                                              ┌────────┴────────┐
     │                                              ↓                 ↓
     │                                        [✓ 수락]          [✕ 거부]
     │                                              │                 │
     │                                     그리드에 값 반영      카드만 제거
     │                                              │                 │
     └──────────────────────────────────────────────┴─────────────────┘
                                          (success Toast 표시)
```

### 9.2 SP 추천 수락 처리

1. 수락 버튼 클릭 시:
   - `requirement.story_points = recommendedSP`
   - `requirement.estimated_md = recommendedSP × ProjectConfig.mdPerSp`
   - `requirement._sp_pending = false`
   - 그리드 해당 행 업데이트 (하이라이트 `_highlight: true` → 1초 후 해제)
   - 추천 카드 제거
   - success Toast

2. 전체 수락 시: 위 과정을 모든 미처리 카드에 순차 적용

### 9.3 역할 추천 수락 처리

1. 수락 버튼 클릭 시:
   - `requirement.assigned_roles = [recommendedRole]`
   - `requirement.estimated_md = calculatedMD` (LLM 추천 공수 또는 SP × mdPerSp)
   - `requirement._role_pending = false`
   - 그리드 해당 행 업데이트
   - 추천 카드 제거

### 9.4 LLM 오류 처리

| 오류 유형 | 처리 방식 |
|---------|---------|
| 네트워크 연결 실패 | Error Toast "LLM 서버에 연결할 수 없습니다. 네트워크를 확인하세요." |
| 응답 파싱 실패 | Error Toast "LLM 응답을 파싱하지 못했습니다. 다시 시도하세요." |
| 세션 ID 오류 | Error Toast "팀 ID가 올바르지 않습니다. 프로젝트 설정을 확인하세요." |
| 타임아웃 (30초 초과) | Error Toast "LLM 응답 시간이 초과되었습니다." |

- 오류 후 해당 버튼 재활성화 (재시도 가능)
- LLM 호출 중에는 동일 버튼 `disabled` 처리

---

## 10. 반응형 레이아웃 처리

### 10.1 브레이크포인트

| 브레이크포인트 | 너비 | 레이아웃 변경 |
|-------------|------|------------|
| Desktop (기본) | ≥ 1280px | 2-column 레이아웃, 드래그 리사이저 |
| Tablet | 768px ~ 1279px | 2-column 유지, 우측 컬럼 최소 너비 축소 |
| Mobile | < 768px | 1-column, 우측 AI 패널 탭 방식으로 전환 |

### 10.2 반응형 규칙 상세

| 요소 | Mobile/Tablet 처리 |
|------|-------------------|
| 헤더 버튼 그룹 | 아이콘만 표시 (레이블 숨김) |
| 탭 레이블 | 아이콘만 표시 (텍스트 숨김) |
| 그리드 컬럼 | 핵심 컬럼(ID, 이름, SP, 역할)만 표시; 나머지 숨김 |
| 2-column 레이아웃 | 1-column 전환; 우측 패널 탭 버튼으로 전환 |
| Toast | `right: 12px; bottom: 12px` (모바일 여백 조정) |
| 드로어 | `max-width: 90vw` 유지 |
| 내부 패딩 | `30px` → `16px` (모바일) |

### 10.3 뷰포트 패딩

```css
.body-content {
  padding: 30px;          /* 기본 (≥768px) */
}
@media (max-width: 767px) {
  .body-content { padding: 16px; }
}
```

---

## 11. 요구사항 추적성 매트릭스

### 11.1 SRS 기능 요구사항 → 화면 매핑

| SRS 요구사항 ID | 요구사항명 | 관련 화면 | 관련 UI 요소 |
|---------------|---------|---------|------------|
| DEV_FR_001 | DEV REQ JSON 파일 불러오기 | SCR-ALL | 헤더 불러오기 버튼, 파일 input |
| DEV_FR_001_01 | JSON 파일 선택 및 파싱 | SCR-ALL | 숨김 `<input type="file">`, 로딩 오버레이 |
| DEV_FR_001_02 | 파싱 오류 처리 및 피드백 | SCR-ALL | Error Toast |
| DEV_FR_001_03 | 로드된 요구사항 그리드 표시 | SCR-02 | 요구사항 산정 그리드 |
| DEV_FR_002 | 요구사항 목록 그리드 뷰 및 CRUD | SCR-02 | 요구사항 산정 그리드 전체 |
| DEV_FR_002_01 | 그리드 계층 표시 | SCR-02 | 들여쓰기 레벨 렌더링 |
| DEV_FR_002_02 | 인라인 편집 | SCR-02 | 셀 클릭 드롭다운 |
| DEV_FR_002_03 | 행 드로어 편집 | SCR-02 | 편집 아이콘 → 인라인 드로어 |
| DEV_FR_003 | 프로젝트 초기 설정 | SCR-03 | 드로어 전체 |
| DEV_FR_003_01 | 기본 정보 입력 | SCR-03 | 프로젝트명, 팀 ID 입력 필드 |
| DEV_FR_003_02 | 일정 및 공수 설정 | SCR-03 | 마감일, 총 공수, MD/SP 계수 |
| DEV_FR_003_03 | 인력 구성 편집 | SCR-03 | 인력 구성 테이블 + 추가/삭제 |
| DEV_FR_004 | 요구사항별 중요도 및 우선순위 설정 | SCR-02 | 중요도/우선순위 배지 드롭다운 |
| DEV_FR_005 | LLM 기반 스토리포인트 자동 추천 | SCR-02 | AI 추천 버튼, 추천 보조 패널 |
| DEV_FR_005_01 | LLM SP 일괄 추천 요청 | SCR-02 | `AI SP 추천` 버튼, 로딩 오버레이 |
| DEV_FR_005_02 | 추천 결과 수락/거부 | SCR-02 | 추천 카드, [수락]/[거부] 버튼 |
| DEV_FR_006 | LLM 기반 역할 배정 및 공수 산출 | SCR-02 | AI 역할 추천 버튼, 추천 패널 |
| DEV_FR_006_01 | LLM 역할 일괄 추천 요청 | SCR-02 | `AI 역할 추천` 버튼 |
| DEV_FR_006_02 | 역할 추천 수락/거부 | SCR-02 | 추천 카드 [수락]/[거부] |
| DEV_FR_007 | 요구사항 간 의존관계 설정 | SCR-02 | 의존관계 열, 의존관계 편집 모달 |
| DEV_FR_007_01 | 선행 요구사항 선택 | SCR-02 | 의존관계 모달 멀티셀렉트 |
| DEV_FR_007_02 | 순환 참조 방지 | SCR-02 | Error Toast, 선택 비활성화 |
| DEV_FR_008_01 | 간트 차트 시각화 | SCR-04 | Mermaid 간트 차트 |
| DEV_FR_008_02 | 프로젝트 대시보드 | SCR-01 | KPI 카드, 분포 차트, 리스크 패널 |
| DEV_FR_008_03 | JSON 내보내기 | SCR-ALL | 헤더 내보내기 버튼 |

### 11.2 SRS 비기능 요구사항 → 화면 매핑

| SRS 요구사항 ID | 요구사항명 | 관련 화면 | 구현 방식 |
|---------------|---------|---------|---------|
| DEV_NFR_001 | RETS 디자인 시스템 준수 | SCR-ALL | CSS 변수, Noto Sans KR, Blur Glass 헤더, 포인트 컬러 `#F97316` |
| DEV_NFR_001_01 | 라이트/다크 모드 | SCR-ALL | `data-theme` 전환, CSS 변수 |
| DEV_NFR_001_02 | 반응형 레이아웃 | 전체 | 브레이크포인트별 레이아웃 조정 |
| DEV_NFR_002 | 한글/영어 다국어 지원 | 전체 | `i18n[lang]` 객체 기반 텍스트 치환 |
| DEV_NFR_003 | 비동기 처리 UI | SCR-ALL | 로딩 오버레이, 스피너, 상태 Toast |
| DEV_NFR_003_01 | File I/O 비동기 | SCR-ALL | 파일 로드 스피너, success/error Toast |
| DEV_NFR_003_02 | LLM 호출 비동기 | SCR-02 | AI 추천 로딩 스피너, 결과 Toast |
| DEV_NFR_004 | LLM 수락/거부 UI | SCR-02 | 추천 카드 수락/거부 버튼 패턴 |
| DEV_NFR_005 | 단일 HTML 파일 | 전체 | 모든 CSS/JS 인라인, Base64 이미지 |

### 11.3 TDD 테스트 케이스 → 화면 매핑

| TDD 테스트 시나리오 | 관련 화면 |
|-----------------|---------|
| TS-01: 파일 I/O | SCR-ALL (헤더 버튼), SCR-02 (그리드 로드) |
| TS-02: 프로젝트 설정 | SCR-03 (드로어 전체) |
| TS-03: 요구사항 관리 | SCR-02 (그리드 CRUD, 배지 변경) |
| TS-04: AI SP 추천 | SCR-02 (AI 추천 버튼, 추천 패널, 수락/거부) |
| TS-05: AI 역할/공수 | SCR-02 (AI 역할 추천, 수락/거부) |
| TS-06: 의존관계 | SCR-02 (의존관계 모달), SCR-04 (간트 순서) |
| TS-07: 간트 차트 | SCR-04 (차트 렌더링, 위상정렬) |
| TS-08: 대시보드 | SCR-01 (KPI 카드, 분포 차트) |
| TS-09: UI/UX | SCR-ALL (테마, Toast, 드로어, 반응형) |
| TS-10: 기술 제약 | 전체 (단일 HTML, localStorage, i18n) |

---

## 변경 이력

| 버전 | 일자 | 작성자 | 변경 내용 |
|------|------|--------|---------|
| 1.0 | 2026-05-07 | 개발팀 | 최초 작성 — SCR-ALL, SCR-01, SCR-02, SCR-03, SCR-04 전체 설계 완료 |

---

*본 문서는 RETS-MODULE-03 개발팀이 작성하였으며, 구현 진행 상황에 따라 내용이 갱신될 수 있다.*
