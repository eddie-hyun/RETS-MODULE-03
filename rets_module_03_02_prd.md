# 제품 요구사항 정의서 (PRD)

**문서명:** 제품 요구사항 정의서 (Product Requirements Document)
**산출물 ID:** MD02
**모듈:** RETS-MODULE-03 — 요구사항 기반 규모 산정 및 투입 계획 도구
**버전:** 1.0
**작성일:** 2026-05-07
**참고 문서:** `/00_rfp_requirements/rets_00_rfp.pdf`

---

## 1. 개요

### 1.1 문서 목적

본 문서는 RETS-MODULE-03의 제품(모듈) 범위, 해결하고자 하는 문제, 대상 사용자, 유저스토리, RFP 기반 요구사항, 기술 고려사항 및 실행 방법을 상위 수준에서 기술한다. 이후 단계에서 작성되는 SRS(MD03), TDD(MD04), 화면 설계서(MD06) 등의 기준 문서로 활용된다.

### 1.2 모듈 한 줄 정의

> **요구사항 목록과 투입 인력 정보를 입력받아, AI 기반 공수 산정과 일정 계획을 지원하는 웹 기반 도구**

---

## 2. 문제 정의

### 2.1 현재 문제 상황

소프트웨어 개발 프로젝트의 제안 및 계획 단계에서 공수 산정과 일정 수립은 높은 불확실성을 가진다. 현재 실무에서는 다음과 같은 문제가 존재한다.

**공수 산정의 주관성과 불투명성:** 요구사항별 난이도(스토리포인트)와 공수 산정이 담당자의 경험과 직관에 의존하며, 산정 근거가 명시적으로 관리되지 않는다.

**일정 계획 수립의 비효율:** 요구사항, 인력 구성, 의존 관계를 연계한 통합 일정 수립 작업이 수작업으로 이루어지며, 변경 발생 시 연쇄 수정이 필요하다.

**역할 배정과 공수의 연계 부재:** 인력 등급별 역할과 예상 공수 간의 논리적 연결이 문서화되지 않아 인력 계획의 근거를 추적하기 어렵다.

**요구사항 기반 계획 수립의 어려움:** 요구사항 정보(중요도, 우선순위, 의존 관계)가 일정 계획에 직접 반영되지 않아 요구사항과 계획 간 정합성이 낮다.

### 2.2 모듈이 제공하는 해결책

RETS-MODULE-03은 개발 요구사항 목록을 입력으로 받아, AI(LLM) 기반 스토리포인트 추천 및 역할 배정을 통해 공수 산정 과정을 체계화하고, 간트 차트 기반 일정 계획 수립을 지원함으로써 계획 단계의 예측 가능성과 근거의 명확성을 향상시킨다.

---

## 3. 대상 사용자

| 사용자 유형 | 설명 | 주요 관심사 |
|-------------|------|-------------|
| **PM / 프로젝트 계획 담당자** | 요구사항 기반 전체 일정과 투입 인력 계획을 수립하는 책임자 | 전체 공수 산정 정확성, 일정 계획 수립, 리스크 가시화 |
| **기술 리드 / 시니어 개발자** | 요구사항별 난이도와 역할 배정을 검토하고 확정하는 역할 | 스토리포인트 검토, 기술적 의존 관계 설정 |
| **제안 담당자** | 사업 제안 단계에서 규모 산정 근거가 필요한 역할 | 요구사항별 공수 근거 문서화, 산정 결과 내보내기 |

### 3.1 주요 사용 시나리오

사용자는 MODULE-01에서 산출된 개발요구사항(DEV REQ) JSON 파일을 불러와, 인력 구성 정보를 입력하고, AI 기반 스토리포인트 추천을 검토하면서 공수와 일정을 수립한 후 최종 결과를 JSON으로 내보낸다.

---

## 4. 제품 범위

### 4.1 In Scope (개발 범위)

- 개발요구사항(DEV REQ) JSON 파일 불러오기 및 파싱
- 프로젝트 초기 설정 (기한, 총 공수, 인력 등급별 인원 수 및 일일 가용 시간)
- 요구사항 목록 그리드 뷰 — 공수 산정 관련 컬럼(중요도, 우선순위, 스토리포인트, 공수, 담당자) UPDATE 가능
- 요구사항별 중요도 및 우선순위 설정
- **[AI]** LLM 기반 스토리포인트 자동 추천 → 사용자 수락/거부
- **[AI]** LLM 기반 담당 역할 배정 및 예상 공수 자동 산출 → 수기 수정 가능
- 요구사항 간 의존(선후행) 관계 설정
- 간트 차트 기반 주차별 일정 계획 시각화
- 프로젝트 대시보드 (전체 현황, 미완료 항목, 리스크 조기 식별)
- 산정 결과가 반영된 DEV REQ 목록 JSON 내보내기
- RETS 공통 디자인 시스템 준수 (라이트/다크 모드, 반응형)
- 한글/영어 다국어 지원

### 4.2 Out of Scope (개발 제외 범위)

- 실시간 협업 기능 (동시 다중 사용자 편집)
- 서버 기반 데이터 영구 저장 (백엔드 DB 없음)
- RFP 요구사항(RFP REQ) → 개발 요구사항(DEV REQ) 변환 기능 (MODULE-01 담당)
- 외부 프로젝트 관리 도구(Jira, Asana 등) 연동
- Skill Set별 세부 등급 분류 (종합 등급으로 단순화)

---

## 5. 유저스토리 (RFP 기반 상세화)

### US-01. 요구사항 목록 불러오기 및 초기 환경 구성

**As a** PM / 기획 담당자  
**I want to** 개발요구사항 JSON 파일을 불러와 산정 작업의 초기 환경을 구성하고 싶다  
**So that** 이미 정의된 요구사항 목록을 기반으로 공수 산정을 즉시 시작할 수 있다

**수락 기준:**
- DEV REQ JSON 파일을 [불러오기] 버튼으로 로드하면 요구사항 목록이 그리드에 표시된다
- 불러온 요구사항은 `rets_02c_req_schema.json` 스키마를 준수한다
- 파일 로드 실패 시 Toast로 오류 메시지를 표시한다
- 공수 산정 관련 컬럼(중요도, 우선순위, 스토리포인트, 담당 역할, 예상 공수) 위주의 편집 가능한 그리드 뷰를 제공한다

---

### US-02. 프로젝트 초기 설정 및 투입 인력 구성

**As a** PM  
**I want to** 전체 기한, 총 공수, 인력 구성(역할·등급·인원 수·일일 가용 시간)을 설정하고 싶다  
**So that** 공수 산정의 기준을 명확히 정의하고 자동 산출의 근거를 확보할 수 있다

**수락 기준:**
- 전체 프로젝트 기한(시작일/종료일) 및 총 공수(MD) 설정이 가능하다
- 인력 역할(개발자, 디자이너, QA 등)과 등급별 인원 수 및 일일 가용 시간 입력이 가능하다
- 시니어 개발자 3명, 주니어 개발자 2명 등 복합 구성 입력을 지원한다
- 설정된 인력 정보를 기반으로 총 가용 공수가 자동 계산되어 표시된다

---

### US-03. 요구사항별 중요도 및 우선순위 설정

**As a** 기술 리드  
**I want to** 각 요구사항에 중요도와 비즈니스 우선순위를 설정하고 싶다  
**So that** 일정 계획 시 우선 처리 순서의 근거를 마련할 수 있다

**수락 기준:**
- 요구사항 목록 그리드에서 중요도(Critical/High/Medium/Low)와 우선순위(MoSCoW)를 인라인 편집 가능하다
- 중요도와 우선순위 값으로 목록 정렬 및 필터링이 가능하다
- 변경된 값은 즉시 저장되어 이후 AI 산정에 반영된다

---

### US-04. AI 기반 스토리포인트 자동 추천 및 수락/거부

**As a** 기술 리드  
**I want to** AI가 요구사항의 내용, 중요도, 우선순위를 분석하여 난이도(스토리포인트)를 추천해 주기를 원한다  
**So that** 스토리포인트 산정의 객관적 기준을 확보하고 수작업 산정 시간을 단축할 수 있다

**수락 기준:**
- [AI 스토리포인트 추천] 버튼 클릭 시 LLM이 분석 후 각 요구사항에 대한 스토리포인트 추천 값을 반환한다
- AI 추천 결과는 요구사항 목록에 임시 표시되며, 사용자가 [수락] 또는 [거부]를 선택할 수 있다
- 수락하지 않은 요구사항은 기존 값이 유지되며, 숫자 직접 입력으로 수기 수정이 가능하다
- AI 호출 중 로딩 스피너가 표시되고, 완료 시 Toast로 결과를 안내한다

---

### US-05. LLM 기반 역할 배정 및 예상 공수 자동 산출

**As a** PM  
**I want to** AI가 스토리포인트와 인력 구성 정보를 바탕으로 각 요구사항의 담당 역할을 배정하고 예상 공수를 산출해 주기를 원한다  
**So that** 역할별 업무 분장과 공수 계획의 초안을 빠르게 확보할 수 있다

**수락 기준:**
- [AI 역할/공수 산출] 버튼 클릭 시 스토리포인트 및 인력 구성 기반으로 담당 역할과 예상 공수(MD)가 자동 산출된다
- AI 추천 결과는 사용자 수락/거부 후 반영되며, 이후 수기 수정이 가능하다
- 전체 공수 합산이 총 가용 공수를 초과할 경우 경고(Warning) 표시를 제공한다

---

### US-06. 의존관계 설정 및 간트 차트 일정 계획

**As a** PM  
**I want to** 요구사항 간 선후행 의존 관계를 설정하고, 이를 바탕으로 주차별 일정을 간트 차트로 확인하고 싶다  
**So that** 논리적으로 올바른 개발 순서와 일정 계획을 수립할 수 있다

**수락 기준:**
- 요구사항 상세 화면에서 선행 요구사항(`predecessor_dev_ids`)을 지정할 수 있다
- 의존 관계, 공수, 역할 배정 정보를 기반으로 주차별 간트 차트가 자동 생성된다
- 간트 차트는 Mermaid.js 라이브러리를 활용하여 표시된다
- 일정이 전체 기한을 초과하는 경우 경고 표시를 제공한다

---

### US-07. 프로젝트 대시보드 조회

**As a** PM  
**I want to** 진행 중인 산정 작업의 전체 현황을 한눈에 파악하고 싶다  
**So that** 미완료 항목과 리스크 요소를 조기에 식별하고 대응할 수 있다

**수락 기준:**
- 대시보드는 모듈의 진입 화면으로 제공된다
- 전체 요구사항 수, 스토리포인트 미배정 항목 수, 공수 미산출 항목 수, 총 예상 공수 대비 가용 공수 등 핵심 지표가 표시된다
- 공수 초과 위험, 의존 관계 미설정 항목 등 잠재 리스크 항목이 강조 표시된다

---

### US-08. 산정 결과 내보내기

**As a** 제안 담당자  
**I want to** 공수 산정 및 계획 정보가 반영된 DEV REQ 목록을 JSON으로 내보내고 싶다  
**So that** 산정 결과를 타 모듈(MODULE-04, MODULE-05 등) 또는 문서화에 활용할 수 있다

**수락 기준:**
- [내보내기] 버튼 클릭 시 `rets_02c_req_schema.json` 스키마를 준수하는 JSON 파일로 다운로드된다
- 내보내기 파일에는 `story_points`, `estimated_effort`, `predecessor_dev_ids`, `developer` 등 산정 관련 필드가 포함된다
- 내보내기 완료 시 Toast로 안내한다

---

## 6. RFP 기반 요구사항 매핑

| RFP 핵심 유저스토리 | 연계 유저스토리 | 우선순위 |
|---------------------|-----------------|----------|
| 요구사항 목록 불러오기 및 프로젝트 초기 설정 | US-01, US-02 | MUST |
| 프로젝트 초기 설정 및 투입 인력 구성 | US-02 | MUST |
| 요구사항별 중요도 및 우선순위 설정 | US-03 | MUST |
| LLM 기반 스토리포인트 자동 추천 및 수락/거부 | US-04 | MUST |
| LLM 기반 담당 역할 배정 및 예상 공수 자동 산출 | US-05 | MUST |
| 일정 계획 수립 (간트 차트) | US-06 | MUST |
| 프로젝트 대시보드 | US-07 | MUST |
| 산정 결과 내보내기 | US-08 | MUST |

---

## 7. 사용자 경험 및 디자인 고려사항

### 7.1 RETS 공통 디자인 시스템 준수

본 모듈은 `rets_02b_design_requirements.md` 및 `rets_02b_design_system_html/02b_design_system_ref.html`에 정의된 RETS 공통 디자인 시스템을 준수한다. 주요 원칙은 다음과 같다.

**시각 스타일:** shadcn/ui 계열의 미니멀한 업무형 인터페이스. 포인트 컬러 `#F97316`. 폰트 `Noto Sans KR`. 라이트/다크 모드 모두 지원.

**화면 구조:** Header — (Working Tabs) — Body 구조. Header는 좌측 모듈명, 중앙 조직 로고, 우측 버튼 그룹(테마 전환, 불러오기, 내보내기)의 3-column 구조.

**인터랙션 원칙:** Alert 사용 금지 → 우측 하단 Toast 사용. 팝업 사용 금지 → Modal 또는 Drawer 사용. LLM 응답은 자동 반영 금지 → 사용자 수락/거부 UI 필수.

### 7.2 화면 구성 방향

**진입 화면 — 프로젝트 대시보드:** 전체 산정 현황 지표, 미완료 항목 목록, 공수 초과 위험 경고.

**요구사항 산정 화면 (2-column):** 좌측에 요구사항 그리드(편집 가능), 우측에 AI 추천 결과 검토 패널 또는 상세 편집 영역.

**일정 계획 화면:** 간트 차트 (Mermaid.js), 주차별 인력 가용 공수 대비 배정 공수 표시.

**초기 설정 Drawer:** 프로젝트 기본 정보 및 인력 구성 입력 패널.

### 7.3 AI UX 고려사항

AI(LLM) 호출 시 비동기 처리 UI(로딩 스피너)를 제공하고, 응답 완료 후 사용자가 개입하여 수락/거부할 수 있도록 한다. AI 추천 결과는 원본 값과 구분하여 표시하며, 거부 시 이전 값으로 롤백된다.

---

## 8. 기술 고려사항

### 8.1 구현 제약사항

| 항목 | 규칙 |
|------|------|
| 구현 형식 | 단일 HTML 파일 (`rets_module_03_07_app.html`) |
| 스크립트/스타일 | HTML 파일 내 통합 (별도 외부 파일 없음) |
| 백엔드 | 없음 (순수 프론트엔드) |
| 스토리지 | LocalStorage/SessionStorage 활용 가능 (자율) |
| 비동기 처리 | 1초 초과 가능 작업(LLM 호출, File I/O)은 모두 비동기 처리 |

### 8.2 LLM 릴레이 서버 활용

LLM 기능은 공용 Gemini 릴레이 서버(`https://ai-relay.mooo.com:11443`)를 통해 호출한다.

**세션 관리:** 페이지 로드 시 `POST /chat/new`로 세션 1회 생성, 이후 재사용. 팀당 동시 활성 세션은 1개로 제한.

**LLM 출력 형식:** 모든 LLM 호출의 user prompt 끝에 JSON 출력 스키마를 명시. `parseLlmJson()` 헬퍼를 통해 마크다운 펜스 제거 및 파싱 처리.

**team_id:** HTML UI를 통해 사용자로부터 입력받아 전역 변수로 관리.

**LLM 활용 영역:**
- 스토리포인트 추천: 요구사항 설명, 중요도, 우선순위를 분석하여 수치 추천
- 역할 배정 및 공수 산출: 스토리포인트와 인력 구성 정보를 기반으로 역할 배정 및 MD 산출

### 8.3 입출력 스키마

**입력:** `rets_02c_req_schema.json` 스키마를 준수하는 DEV REQ JSON (MODULE-01 또는 벤치마크 파일 활용)

**출력:** 동일 스키마에 공수 산정 관련 필드(`story_points`, `estimated_effort`, `predecessor_dev_ids`, `developer`, `dev_status`)가 채워진 DEV REQ JSON

### 8.4 간트 차트

Mermaid.js (`https://mermaid.ai/open-source/syntax/gantt.html`) 라이브러리를 활용하여 주차별 간트 차트를 렌더링한다. 의존 관계 및 공수 정보를 기반으로 Mermaid Gantt 문법으로 자동 변환하여 표시한다.

### 8.5 다국어 지원

`i18n` 객체(한국어/영어 키-값 쌍) 기반으로 UI 레이블 및 메시지 다국어 처리. 헤더의 언어 전환 버튼으로 전환 가능.

---

## 9. 실행 방법 및 제약사항

### 9.1 실행 방법

1. `rets_module_03_07_app.html` 파일을 브라우저에서 직접 열거나 GitHub Pages에 배포된 URL에 접근한다.
2. 헤더의 [불러오기] 버튼으로 DEV REQ JSON 파일을 로드한다. (벤치마크: `rets_benchmark_02_dev_req.json`)
3. [초기 설정] Drawer에서 프로젝트 기한 및 인력 구성을 입력한다.
4. 요구사항 산정 화면에서 중요도/우선순위 설정 후 AI 스토리포인트 추천을 수행하고 검토한다.
5. AI 역할/공수 산출을 실행하고 결과를 수락/거부 후 수기 수정한다.
6. 간트 차트 화면에서 일정 계획을 검토한다.
7. [내보내기] 버튼으로 산정 완료된 DEV REQ JSON 파일을 다운로드한다.

### 9.2 주요 제약사항

- 백엔드 서버 없이 동작하므로 브라우저 탭을 닫으면 미저장 데이터는 손실될 수 있다 (LocalStorage 활용으로 일부 보완 가능).
- LLM 기능은 릴레이 서버 연결이 필요하며, 서버 미연결 시 AI 기능은 비활성화된다.
- 단일 HTML 파일로 제공되므로 추후 상위 RETS 통합 앱에서 iframe 형식으로 임베드된다.
- 팀당 LLM 세션은 동시 1개로 제한되므로, 병렬 AI 호출 시 순차 처리 방식을 사용한다.

---

## 10. 성공 기준

본 모듈의 PoC가 성공적으로 완료된 것으로 판단하기 위한 기준은 다음과 같다.

| 기준 | 내용 |
|------|------|
| 입력 호환성 | 공통 벤치마크 파일(`rets_benchmark_02_dev_req.json`)을 오류 없이 로드할 수 있다 |
| AI 기능 동작 | LLM 기반 스토리포인트 추천이 정상 작동하며 수락/거부 UI가 동작한다 |
| 공수 산출 | 인력 구성 기반 예상 공수가 자동 산출되며 수기 수정이 가능하다 |
| 간트 차트 | 의존 관계 및 공수 기반 간트 차트가 정상 렌더링된다 |
| 출력 호환성 | 산정 결과가 반영된 DEV REQ JSON 파일이 스키마에 맞게 내보내진다 |
| 디자인 준수 | RETS 디자인 시스템이 적용되고 라이트/다크 모드 전환이 동작한다 |
| 다국어 지원 | 한국어/영어 전환이 정상 동작한다 |

---

*본 문서는 RETS-MODULE-03 개발팀이 작성하였으며, SRS(MD03) 작성 시 본 문서를 기준으로 기능 요구사항을 상세화한다.*
